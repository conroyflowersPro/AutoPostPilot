# 변경 기록

버그 수정이나 사용 중 느껴지는 변화가 있으면 **버전을 올리고**, 앱 위쪽에 `vX.Y.Z`로 보이게 합니다.

- **패치** (`11.0.1`): 같은 의도, 버그 수정
- **마이너** (`11.1.0`): 시드·할당량·말투처럼 동작이 바뀜

숫자 출처: `package.json` = `lib/version.ts` = weekly-plan `APP_VERSION`

## 11.2.3 — 2026-08-14

- 경험 증거가 없는 시드는 버리지 않고 정보글로 바꿉니다. 남은 시드로 빈 칸을 채웁니다
- 원글은 한 줄 메모가 아닙니다. `ent ent ent` 같은 반복·너무 짧은 글은 저장하지 않고 한 번 다시 씁니다

## 11.2.2 — 2026-08-14

- 22/28처럼 빈 칸이 있어도 주 전체를 버리지 않습니다. 있는 초안은 저장하고 리뷰로 갑니다
- 빈 칸은 관심사보다 한 칸 밖(전기차·반도체·우주 등) 바이럴 시드로 채웁니다. 유형 비율은 고정하지 않습니다
- 이런 확장 글은 하루 최대 2개입니다. 그래도 부족하면 그 칸은 작성하지 않습니다
- 경험 없는 칸을 가짜 경험으로 메우지는 않습니다

## 11.2.1 — 2026-08-14

- 최근 원글에서 경험 각도를 뽑아 주간 작업 시드에 연결합니다
- 야간 FSD처럼 있는 경험은 인용해서 관련 글을 씁니다. 같은 내용 복제는 금지입니다
- 8/1–8/14 성과·수익은 **후보 패턴**으로만 저장합니다. 원글 문장은 저장하지 않습니다
- 경험 없는 칸을 정보/의견으로 메우지는 않았습니다

## 11.2.0 — 2026-08-14

- 주간 텍스트 생성은 서버 `generation_jobs`에 남기고, 엣지는 한 번에 한 틱만 돕니다
- 화면은 `job_start` 후 `job_tick`을 따라갑니다. 초안은 작성 틱마다 저장됩니다
- 영상 파이프라인은 넣지 않았습니다

## 11.1.6 — 2026-08-14

- 초기라 학습이 얕은 것은 정상. DNA로 이번 주를 채우고, 데이터가 쌓이면 각도를 좁힘
- 화면 `학습:` 줄. 빈 데이터 때문에 시드 0개를 내는 지시를 제거
- 서버 60초 동기 호출은 주간 전체·영상에는 맞지 않음. 다음은 백그라운드 작업

## 11.1.5 — 2026-08-14

- 할당량 Grok은 됐는데 시드가 0/28. 한 번에 10개·토큰 2200이면 JSON이 비거나 잘림
- 시드 배치 6개, 출력 토큰을 늘림. 실패 이유를 회색 칸에 바로 보여 줌

## 11.1.4 — 2026-08-14

- 원인: Grok 4.6 기본 추론이 high라서 18초 안에 JSON이 안 나옴. 할당량은 cadence fallback, 시드는 0/28
- 시드·할당량·작성에 `reasoning_effort: low`. 실패 원인을 화면에 보여 줌. 템플릿으로 채우지는 않음

## 11.1.3 — 2026-08-14

- 포스트·시드가 늘어나도 한 요청을 키우지 않습니다 (시드 10개, 작성 2개).
- 주간 목표가 28이든 최대 56(하루 8×7일)이든, 횟수만 늘려 채웁니다. 한 라운드가 실패해도 전체를 버리지 않습니다.

## 11.1.2 — 2026-08-14

- 원인: 할당량(28) 추론 후 시드 한 요청에서 Grok을 두 번(28초+28초) 불러 엣지 60초 제한에 걸림. 초안 저장 전에 화면이 멈춤
- 시드 배치를 작게, 같은 요청에서 재시도하지 않음. 화면은 45초에 에러를 보여 멈추지 않음. 초안은 된 것부터 저장

## 11.1.1 — 2026-08-14

- 사이트 배포가 깨지던 빌드 오류를 고쳤습니다 (계획 화면 중복 상수, 댓글 제안/다듬기 라우트)
- 댓글 제안·다듬기는 v11에서 계속 닫혀 있습니다 (410)

## 11.1.0 — 2026-08-14

- 시드를 고정 8개 축이 아니라 Creator DNA·엔진·학습 데이터로 추론합니다
- 주간 할당량도 DNA·엔진에서 추론한 뒤, Grok이 그 수를 채웁니다
- 의지는 생성칸이 아니라 엔진과 DNA에 있습니다
- 앱 헤더에 버전을 크게 표시합니다

## 11.0.0 — 2026-08-14

- v11 제품 방향 잠금 (ORDER 0–8, Grok 4.6, 리뷰·원본 미디어 후 발행)
