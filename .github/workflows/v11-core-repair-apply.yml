name: v11-core-repair-apply
on:
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: node tools/v11-core-repair-parts/apply-from-zip-parts.mjs
      - run: |
          git config user.name conroyflowersPro
          git config user.email conroyf12@gmail.com
          git add app/generate/page.tsx supabase/functions/weekly-plan/
          git status
          git commit -m "v11.0.0 CORE-REPAIR apply: phased_v11_order8d_apply" || exit 0
          git push origin main
