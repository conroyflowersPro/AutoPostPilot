name: ORDER7A materialize index
on:
  push:
    branches: [order7a-deep-generation-context]
    paths:
      - "tools/order7a-index.patch"
      - ".github/workflows/order7a-materialize.yml"
      - "supabase/functions/weekly-plan/deep-generation-context.ts"
jobs:
  materialize:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: order7a-deep-generation-context
      - name: Apply index patch
        run: |
          set -e
          if grep -q 'order7a_deep_generation\|const deep_generation' supabase/functions/weekly-plan/index.ts; then
            echo already_materialized
          else
            patch -p1 --forward < tools/order7a-index.patch
          fi
          grep -n 'order7a\|10.0.0-order7a\|buildDeepGenerationContext\|deep_generation' supabase/functions/weekly-plan/index.ts | head -20
          test -f supabase/functions/weekly-plan/deep-generation-context.ts
          wc -c supabase/functions/weekly-plan/index.ts supabase/functions/weekly-plan/deep-generation-context.ts
      - name: Commit
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add supabase/functions/weekly-plan/index.ts || true
          if git diff --cached --quiet; then echo no changes; else
            git commit -m "ORDER7A: materialize deep generation context wiring"
            git push
          fi
